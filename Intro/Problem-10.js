// Write a Prgram to convert the Fahrenheit to Celsius

// Celsius = (Fahrenheit -32 ) /1.8

let fahrenheit = prompt("Enter fahrenheit Value:");

let celsuis = (fahrenheit - 32) / 1.8;

console.log(`${fahrenheit} fahrenheit is equal to ${celsuis} celsius`);
